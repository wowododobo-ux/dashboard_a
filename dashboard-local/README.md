# 📊 Dashboard - 本機開發版

本資料夾專門用於**本機開發和測試**。

## 🎯 用途

此版本針對本機開發環境優化，配置為在 localhost 上運行。

## 🚀 快速開始

### 安裝依賴

```bash
npm install
```

### 啟動開發服務器

```bash
npm run dev
```

瀏覽器將自動開啟 http://localhost:5173

### 構建測試

```bash
npm run build
npm run preview
```

## ⚙️ 配置說明

### vite.config.js 差異

- `base: '/'` - 使用根路徑（本機開發專用）
- `server.port: 5173` - 開發服務器端口
- `server.open: true` - 自動開啟瀏覽器

## 📝 開發流程

1. 在此資料夾進行所有開發工作
2. 測試功能正常後
3. 將修改同步到 `dashboard-vercel` 進行部署

## 🔄 同步到 Vercel 版本

當你在本機開發完成並測試通過後，需要手動同步修改到 dashboard-vercel：

```bash
# 從專案根目錄執行
# 同步 src 資料夾
rsync -av --delete dashboard-local/src/ dashboard-vercel/src/

# 同步 public 資料夾（如果有數據更新）
rsync -av --delete dashboard-local/public/ dashboard-vercel/public/

# 同步 package.json（如果有依賴更新）
cp dashboard-local/package.json dashboard-vercel/package.json
```

注意：不要同步 vite.config.js，兩個版本的配置不同！

## 📦 資料夾結構

```
dashboard-local/
├── src/
│   ├── components/      # 組件開發
│   ├── pages/          # 頁面開發
│   ├── utils/          # 工具函數
│   ├── config/         # 配置文件
│   └── hooks/          # 自定義 Hooks
├── public/             # 靜態資源和數據文件
├── vite.config.js      # 本機開發配置
├── package.json
└── README.md          # 本文件
```

## 💡 開發建議

- 所有新功能先在此版本開發和測試
- 確保本機運行正常後再同步到 Vercel 版本
- 使用 Git 進行版本控制
- 定期備份重要數據文件

## 🐛 常見問題

**Q: 為什麼要分開兩個資料夾？**

A: 因為本機開發和 Vercel 部署的配置不同：
- 本機使用 `base: '/'` 並在 localhost 運行
- Vercel 需要生產環境優化配置
- 分開可以避免配置衝突，更容易管理

**Q: 如何更新依賴？**

```bash
npm install <package-name>
# 然後記得同步 package.json 到 dashboard-vercel
```

**Q: 開發服務器無法啟動？**

```bash
# 清除快取重新安裝
rm -rf node_modules package-lock.json
npm install
npm run dev
```

---

**最後更新**: 2025-11-03
