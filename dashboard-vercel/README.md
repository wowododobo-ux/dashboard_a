# 📊 Dashboard - Vercel 部署版

本資料夾專門用於**Vercel 生產環境部署**。

## 🎯 用途

此版本針對 Vercel 生產環境優化，包含生產環境專用配置。

## ⚠️ 重要提醒

**請勿直接在此資料夾進行開發！**

所有開發工作應在 `dashboard-local` 完成，測試通過後再同步到此資料夾。

## 🚀 部署流程

### 方法 1：Vercel CLI 部署（推薦）

```bash
# 安裝依賴
npm install

# 構建專案
npm run build

# 部署到 Vercel
vercel --prod
```

### 方法 2：Git 自動部署

1. 將代碼推送到 GitHub
2. 在 Vercel 控制台導入倉庫
3. 設定構建配置：
   - **Root Directory**: `dashboard-vercel`
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
4. 完成！每次推送都會自動部署

## ⚙️ 配置說明

### vite.config.js 差異

- `base: '/'` - Vercel 使用根路徑部署
- `outDir: 'dist'` - 輸出目錄設定
- 生產環境優化配置

### Vercel 專案配置

專案根目錄的 `vercel.json` 已配置好路由和重定向規則。

## 🔄 從 Local 版本同步更新

當 `dashboard-local` 開發完成並測試通過後：

```bash
# 從專案根目錄執行
cd /Users/hunsonchen/myenv_all/tk_dashboard_env/dashboard_a

# 同步 src 資料夾
rsync -av --delete dashboard-local/src/ dashboard-vercel/src/

# 同步 public 資料夾
rsync -av --delete dashboard-local/public/ dashboard-vercel/public/

# 同步 package.json（如有依賴更新）
cp dashboard-local/package.json dashboard-vercel/package.json

# 安裝依賴並構建
cd dashboard-vercel
npm install
npm run build

# 部署
vercel --prod
```

**注意**：不要覆蓋 `vite.config.js`，兩個版本的配置不同！

## 📦 部署檢查清單

部署前請確認：

- [ ] 在 `dashboard-local` 測試通過
- [ ] 所有功能正常運作
- [ ] 數據文件已更新（如需要）
- [ ] package.json 依賴已同步
- [ ] 本地構建成功（`npm run build`）
- [ ] 檢查構建輸出無錯誤

## 📊 數據文件更新

如需更新數據文件：

1. 在 `dashboard-local/public/` 更新 Excel 檔案
2. 在本機測試確認數據正確載入
3. 使用 rsync 同步到 `dashboard-vercel/public/`
4. 重新構建並部署

## 🔍 監控與維護

### 查看部署狀態

訪問 Vercel Dashboard 查看：
- 部署歷史
- 構建日誌
- 訪問統計
- 錯誤報告

### 回滾版本

如果部署出現問題，可在 Vercel Dashboard 快速回滾到上一個版本。

## 🐛 常見問題

**Q: 部署失敗怎麼辦？**

1. 檢查 Vercel 構建日誌
2. 確認本地構建成功（`npm run build`）
3. 檢查依賴版本是否相容
4. 確認所有必要文件都已提交

**Q: 部署後頁面空白？**

1. 檢查瀏覽器控制台錯誤
2. 確認 `vite.config.js` 的 `base` 設定正確
3. 檢查路由配置
4. 確認數據文件路徑正確

**Q: 如何更新環境變數？**

在 Vercel Dashboard 的專案設定中配置環境變數。

## 📝 維護建議

- 定期檢查依賴更新
- 監控部署日誌
- 定期備份重要數據
- 保持與 `dashboard-local` 同步

## 🌐 相關連結

- [Vercel Dashboard](https://vercel.com/dashboard)
- [Vercel 文檔](https://vercel.com/docs)
- [Vite 部署指南](https://vitejs.dev/guide/static-deploy.html)

---

**最後更新**: 2025-11-03
**環境**: Vercel 生產環境
